# ==============================================
# Smart Flow Metering Docker Compose - Dokploy Configuration
# ==============================================
# This file is optimized for deployment with Dokploy.
# 
# Key features:
# - Traefik integration for automatic SSL
# - Dokploy network for service discovery
# - Environment variable management via Dokploy UI
# - Health checks for all services
# - Automated backups with retention policy
# ==============================================

services:
  # ==========================================
  # Admin Dashboard
  # ==========================================
  admin-dashboard:
    build:
      context: ./apps/admin-dashboard
      dockerfile: Dockerfile
      args:
        # Set in Dokploy environment variables:
        # VITE_API_URL: https://api.smartmetering.africa
        VITE_API_URL: ${VITE_API_URL:-https://api.smartmetering.africa}
    container_name: smartflowmetering-admin-dashboard
    restart: unless-stopped
    networks:
      - backend
      - dokploy-network
    # Traefik labels for automatic SSL (configure domain in Dokploy UI)
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.smartflowmetering-admin-dashboard.rule=Host(`${WEB_DOMAIN:-smartmetering.africa}`) || Host(`${WEB_DOMAIN_WWW:-www.smartmetering.africa}`)"
      - "traefik.http.routers.smartflowmetering-admin-dashboard.entrypoints=websecure"
      - "traefik.http.routers.smartflowmetering-admin-dashboard.tls.certresolver=letsencrypt"
      - "traefik.http.services.smartflowmetering-admin-dashboard.loadbalancer.server.port=3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ==========================================
  # API (Smart Flow Metering Backend)
  # ==========================================
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: smartflowmetering-api
    restart: unless-stopped
    networks:
      - backend
      - dokploy-network
    # Traefik labels for automatic SSL (configure domain in Dokploy UI)
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.smartflowmetering-api.rule=Host(`${API_DOMAIN:-api.smartmetering.africa}`)"
      - "traefik.http.routers.smartflowmetering-api.entrypoints=websecure"
      - "traefik.http.routers.smartflowmetering-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.smartflowmetering-api.loadbalancer.server.port=3000"
    environment:
      # Application Logic
      NODE_ENV: production
      PORT: 3000
      SFM_PROCESS_ROLE: api

      # Internal Network Connection
      DATABASE_URL: ${DATABASE_URL:-postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-smartflowmetering}}
      REDIS_URL: ${REDIS_URL:-redis://:${REDIS_PASSWORD}@redis:6379}

      # External Logic (Set in Dokploy environment variables)
      MPESA_SHORTCODE: ${MPESA_SHORTCODE}
      MPESA_CONSUMER_KEY: ${MPESA_CONSUMER_KEY}
      MPESA_CONSUMER_SECRET: ${MPESA_CONSUMER_SECRET}
      MPESA_PASSKEY: ${MPESA_PASSKEY}
      MPESA_ENVIRONMENT: ${MPESA_ENVIRONMENT}
      MPESA_BASE_URL: ${MPESA_BASE_URL}
      MPESA_CALLBACK_URL: ${MPESA_CALLBACK_URL}
      MPESA_REGISTER_TOKEN: ${MPESA_REGISTER_TOKEN}
      MPESA_CALLBACK_TOKEN: ${MPESA_CALLBACK_TOKEN}
      MPESA_CALLBACK_TOKEN_TRANSPORT: ${MPESA_CALLBACK_TOKEN_TRANSPORT}
      MPESA_SIGNATURE_SECRET: ${MPESA_SIGNATURE_SECRET}
      MPESA_SIGNATURE_HEADER: ${MPESA_SIGNATURE_HEADER}
      MPESA_SIGNATURE_TIMESTAMP_HEADER: ${MPESA_SIGNATURE_TIMESTAMP_HEADER}
      MPESA_SIGNATURE_MAX_AGE_SECONDS: ${MPESA_SIGNATURE_MAX_AGE_SECONDS}
      MPESA_REQUIRE_SIGNATURE: ${MPESA_REQUIRE_SIGNATURE}
      MPESA_C2B_COMMAND_ID: ${MPESA_C2B_COMMAND_ID}
      MPESA_TRANSACTION_STATUS_COMMAND_ID: ${MPESA_TRANSACTION_STATUS_COMMAND_ID}
      MPESA_IDENTIFIER_TYPE: ${MPESA_IDENTIFIER_TYPE}
      MPESA_ALLOWED_IPS: ${MPESA_ALLOWED_IPS}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID}
      FIREBASE_SERVICE_ACCOUNT_PATH: ${FIREBASE_SERVICE_ACCOUNT_PATH}
      FCM_ENABLED: ${FCM_ENABLED}
      FCM_DRY_RUN: ${FCM_DRY_RUN}
      HOSTPINNACLE_API_URL: ${HOSTPINNACLE_API_URL}
      HOSTPINNACLE_USER_ID: ${HOSTPINNACLE_USER_ID}
      HOSTPINNACLE_PASSWORD: ${HOSTPINNACLE_PASSWORD}
      HOSTPINNACLE_API_KEY: ${HOSTPINNACLE_API_KEY}
      HOSTPINNACLE_SENDER_ID: ${HOSTPINNACLE_SENDER_ID}
      HOSTPINNACLE_DLR_WEBHOOK_TOKEN: ${HOSTPINNACLE_DLR_WEBHOOK_TOKEN}
      HOSTPINNACLE_DLR_WEBHOOK_HEADER: ${HOSTPINNACLE_DLR_WEBHOOK_HEADER}
      GOMELONG_API_URL: ${GOMELONG_API_URL}
      GOMELONG_USER_ID: ${GOMELONG_USER_ID}
      GOMELONG_PASSWORD: ${GOMELONG_PASSWORD}
      GOMELONG_VENDING_TYPE: ${GOMELONG_VENDING_TYPE}
      ALERT_AUTOMATION_ENABLED: ${ALERT_AUTOMATION_ENABLED}
      ALERT_AUTOMATION_INTERVAL_SECONDS: ${ALERT_AUTOMATION_INTERVAL_SECONDS}
      ALERT_TIMEZONE: ${ALERT_TIMEZONE}
      LOW_BALANCE_ALERT_DEDUPE_HOURS: ${LOW_BALANCE_ALERT_DEDUPE_HOURS}
      POSTPAID_REMINDER_DEDUPE_HOURS: ${POSTPAID_REMINDER_DEDUPE_HOURS}
      POSTPAID_REMINDER_DAYS_AFTER_PAYMENT: ${POSTPAID_REMINDER_DAYS_AFTER_PAYMENT}
      LANDLORD_DAILY_USAGE_SMS_ENABLED: ${LANDLORD_DAILY_USAGE_SMS_ENABLED}
      LANDLORD_DAILY_USAGE_SMS_HOUR: ${LANDLORD_DAILY_USAGE_SMS_HOUR}

      # CORS - include your production domains
      CORS_ORIGINS: "https://smartmetering.africa,https://www.smartmetering.africa,https://api.smartmetering.africa"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  worker:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: smartflowmetering-worker
    restart: unless-stopped
    networks:
      - backend
    environment:
      NODE_ENV: production
      SFM_PROCESS_ROLE: worker
      DATABASE_URL: ${DATABASE_URL:-postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-smartflowmetering}}
      REDIS_URL: ${REDIS_URL:-redis://:${REDIS_PASSWORD}@redis:6379}
      MPESA_SHORTCODE: ${MPESA_SHORTCODE}
      MPESA_CONSUMER_KEY: ${MPESA_CONSUMER_KEY}
      MPESA_CONSUMER_SECRET: ${MPESA_CONSUMER_SECRET}
      MPESA_PASSKEY: ${MPESA_PASSKEY}
      MPESA_ENVIRONMENT: ${MPESA_ENVIRONMENT}
      MPESA_BASE_URL: ${MPESA_BASE_URL}
      MPESA_CALLBACK_URL: ${MPESA_CALLBACK_URL}
      MPESA_REGISTER_TOKEN: ${MPESA_REGISTER_TOKEN}
      MPESA_CALLBACK_TOKEN: ${MPESA_CALLBACK_TOKEN}
      MPESA_CALLBACK_TOKEN_TRANSPORT: ${MPESA_CALLBACK_TOKEN_TRANSPORT}
      MPESA_SIGNATURE_SECRET: ${MPESA_SIGNATURE_SECRET}
      MPESA_SIGNATURE_HEADER: ${MPESA_SIGNATURE_HEADER}
      MPESA_SIGNATURE_TIMESTAMP_HEADER: ${MPESA_SIGNATURE_TIMESTAMP_HEADER}
      MPESA_SIGNATURE_MAX_AGE_SECONDS: ${MPESA_SIGNATURE_MAX_AGE_SECONDS}
      MPESA_REQUIRE_SIGNATURE: ${MPESA_REQUIRE_SIGNATURE}
      MPESA_C2B_COMMAND_ID: ${MPESA_C2B_COMMAND_ID}
      MPESA_TRANSACTION_STATUS_COMMAND_ID: ${MPESA_TRANSACTION_STATUS_COMMAND_ID}
      MPESA_IDENTIFIER_TYPE: ${MPESA_IDENTIFIER_TYPE}
      MPESA_ALLOWED_IPS: ${MPESA_ALLOWED_IPS}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID}
      FIREBASE_SERVICE_ACCOUNT_PATH: ${FIREBASE_SERVICE_ACCOUNT_PATH}
      FCM_ENABLED: ${FCM_ENABLED}
      FCM_DRY_RUN: ${FCM_DRY_RUN}
      HOSTPINNACLE_API_URL: ${HOSTPINNACLE_API_URL}
      HOSTPINNACLE_USER_ID: ${HOSTPINNACLE_USER_ID}
      HOSTPINNACLE_PASSWORD: ${HOSTPINNACLE_PASSWORD}
      HOSTPINNACLE_API_KEY: ${HOSTPINNACLE_API_KEY}
      HOSTPINNACLE_SENDER_ID: ${HOSTPINNACLE_SENDER_ID}
      HOSTPINNACLE_DLR_WEBHOOK_TOKEN: ${HOSTPINNACLE_DLR_WEBHOOK_TOKEN}
      HOSTPINNACLE_DLR_WEBHOOK_HEADER: ${HOSTPINNACLE_DLR_WEBHOOK_HEADER}
      GOMELONG_API_URL: ${GOMELONG_API_URL}
      GOMELONG_USER_ID: ${GOMELONG_USER_ID}
      GOMELONG_PASSWORD: ${GOMELONG_PASSWORD}
      GOMELONG_VENDING_TYPE: ${GOMELONG_VENDING_TYPE}
      ALERT_AUTOMATION_ENABLED: ${ALERT_AUTOMATION_ENABLED}
      ALERT_AUTOMATION_INTERVAL_SECONDS: ${ALERT_AUTOMATION_INTERVAL_SECONDS}
      ALERT_TIMEZONE: ${ALERT_TIMEZONE}
      LOW_BALANCE_ALERT_DEDUPE_HOURS: ${LOW_BALANCE_ALERT_DEDUPE_HOURS}
      POSTPAID_REMINDER_DEDUPE_HOURS: ${POSTPAID_REMINDER_DEDUPE_HOURS}
      POSTPAID_REMINDER_DAYS_AFTER_PAYMENT: ${POSTPAID_REMINDER_DAYS_AFTER_PAYMENT}
      LANDLORD_DAILY_USAGE_SMS_ENABLED: ${LANDLORD_DAILY_USAGE_SMS_ENABLED}
      LANDLORD_DAILY_USAGE_SMS_HOUR: ${LANDLORD_DAILY_USAGE_SMS_HOUR}
      CORS_ORIGINS: "https://smartmetering.africa,https://www.smartmetering.africa,https://api.smartmetering.africa"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ==========================================
  # Database (PostgreSQL 18)
  # ==========================================
  postgres:
    image: postgres:18.3-alpine3.22
    container_name: smartflowmetering-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-smartflowmetering}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      # Use Docker Named Volume for Dokploy backup compatibility
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-smartflowmetering}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==========================================
  # Database Admin (Adminer)
  # ==========================================
  # NOTE: Enabled only with --profile ops for internal troubleshooting
  adminer:
    image: adminer:5.4.2-standalone
    container_name: smartflowmetering-adminer
    restart: unless-stopped
    profiles: ["ops"]
    networks:
      - backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres

  # ==========================================
  # Queue (Redis)
  # ==========================================
  redis:
    image: redis:8.4.0-alpine3.22
    container_name: smartflowmetering-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a ${REDIS_PASSWORD} ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==========================================
  # Automated Database Backups
  # ==========================================
  # Creates automatic backups with configurable retention:
  # - Every 15 minutes for 7 days (financial data protection)
  # - Daily backups kept for 4 weeks  
  # - Weekly backups kept for 6 months
  # - Monthly backups kept for 1 year
  # ==========================================
  backup:
    image: prodrigestivill/postgres-backup-local:18
    container_name: smartflowmetering-backup
    restart: unless-stopped
    volumes:
      # Use named volume for Dokploy backup management
      - backup_data:/backups
    networks:
      - backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Database connection
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-smartflowmetering}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      
      # Backup schedule (cron format)
      # Every 15 minutes - for financial data, minimize data loss risk
      # Max data loss: 15 min = ~150 transactions at 10tx/min
      SCHEDULE: "*/15 * * * *"
      
      # Retention policy (adjusted for 15-min backups)
      BACKUP_KEEP_HOURS: 672         # Keep 15-min backups for 7 days (96/day * 7)
      BACKUP_KEEP_DAYS: 28           # Keep daily backups for 4 weeks
      BACKUP_KEEP_WEEKS: 24          # Keep weekly backups for 6 months
      BACKUP_KEEP_MONTHS: 12         # Keep monthly backups for 1 year
      
      # Compression and naming
      BACKUP_SUFFIX: .sql.gz
      POSTGRES_EXTRA_OPTS: --clean --if-exists --no-owner --no-acl
      
      # Health check URL (optional - for external monitoring)
      # HEALTHCHECK_URL: https://hc-ping.com/your-uuid

      # Cloudflare R2 Remote Backup (S3 Compatible)
      # These must be set in Dokploy Environment Variables
      S3_BUCKET: ${R2_BUCKET}
      S3_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      S3_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      S3_ENDPOINT: https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com
      S3_REGION: auto
      S3_PATH: backups

# ==========================================
# Networks
# ==========================================
networks:
  backend:
    driver: bridge
  # Dokploy's external network for Traefik routing
  dokploy-network:
    external: true

# ==========================================
# Volumes (Docker Named Volumes for persistence)
# ==========================================
volumes:
  postgres_data:
    name: smartflowmetering_postgres_data
  redis_data:
    name: smartflowmetering_redis_data
  backup_data:
    name: smartflowmetering_backup_data
